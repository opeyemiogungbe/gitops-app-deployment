apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  template:
    spec:
      containers:
        - name: app-container
          image: myapp:latest
          env:
            - name: APP_ENV
              value: production
            - name: LOG_LEVEL
              value: warn
